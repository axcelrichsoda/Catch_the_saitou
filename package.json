{
  "name": "erroroid",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b --verbose",
    "build": "npm run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.1.4"
  }
}
