# 齋藤さん デジタル対戦版 実装計画

## 進捗状況(2026-07-09時点)

- **ステップ1〜3(盤面・トークン基盤/カード効果システム/通信・同期)**: 完了。Vitestで124件のテストが通っている
- **ステップ4(UI/UX)**: 実装済み。ユーザーによる実ブラウザでの試遊を経て、当初計画になかった改善を多数実施(下記)
- **ステップ5(デプロイ・統合テスト・調整)**: 未着手

### 実装中に確定した仕様変更・修正

- **リヴィールカード「真相解明」の対象は「自分→相手の場」に確定**(本ドキュメント下部のカード効果テキスト表内は当初「自分→自分の場」だったが誤り。相手の場をオープンする仕様が正)
- バグ修正: Express個別ルート(`/rooms`等)にCORSミドルウェアが無くルーム作成が失敗する不具合、同一ブラウザの別タブでsessionTokenが衝突し座席を奪い合う不具合(→`sessionStorage`化+サーバー側`seat_taken`ガード)、React StrictModeの二重effect実行で1ソケットが2座席を同時取得してしまう不具合(→`RoomManager.join`の冪等化)、対戦相手側の操作ログ・通知が自分にしか届いていなかった配信漏れ、をそれぞれ修正済み
- UI/UXの追加改善: カード効果テキスト・コストの常時表示、証拠/アリバイの可視化バー、メモリー/消費/リヴィールを独立したエリアとして表示、手札は「クリックで詳細確認→使用する」の2段階確定に変更、オリバーは自発オープンを不可にして常在トリガー発動時のみ全員へ目立つ確認モーダルを表示、盤面レイアウトを実物のプレイマット写真(`ルール/`フォルダの写真)に合わせて再設計(相手を上/自分を下、ルール早見表パネルを追加、7キャラ横1列固定+カード画像用プレースホルダー確保)
- カード画像(`packages/client/src/assets/cards/`)は未着手。実物写真からの切り出しはClaude Code側に画像編集手段がないため、ユーザー側で用意するか、Google AI Studio(Gemini画像編集)等の外部ツールを使う方針で保留中
- **(2026-07-25)全カードの実物照合が完了し、confidenceは全てconfirmed**。この過程で3件のロジックバグを発見・修正: ①lucas(齋藤さん(弁護士))の1段目対象が「相手の場」ではなく「自分の場」だった、②oliver(齋藤さん(マジシャン))の常在トリガー後の証拠+1が両プレイヤーの場ではなく「トリガー所有者から見た相手の場のみ」だった、③痕跡発見の2段目対象が「相手の場」ではなく「自分自身の場」だった。あわせてキャラクター表示名を実物カード名(齋藤さん(探偵)/(弁護士)/(美大生)/(バーテンダー)/(メイド)/(マジシャン)/(猫))に、「挙動不審」を「単独不審」に更新(コード内部の識別子は変更なし、`docs/cards.md`に対応表あり)

## Context

ユーザーは市販の2人用正体隠匿系推理カードゲーム「エラーロイド」の現物(ルールブック写真8枚、カードデザイン写真4枚がプロジェクトフォルダに保存済み)を持っており、これを友人と2台のPCでリアルタイム対戦できるデジタル版として遊びたい、という依頼。プロジェクトフォルダには現状コードは一切なく、画像資料のみの状態からゼロで開発を始める。

要件確認の結果:
- **対戦形式**: 2台のPCでリアルタイム対戦。当初は同じ部屋のLAN内を想定していたが、実際には2台が別々のLAN(有線・別回線)に接続されているため、プライベートIP直打ちでは繋がらないことが判明。**常時稼働のクラウドデプロイ**(固定URLを友人に伝えてブラウザで開くだけで遊べる方式)を採用することに変更
- **自動化レベル**: フルデジタル化(カード効果・証拠/推理力・山札・勝敗判定まですべてプログラムが管理し、プレイヤーはUI操作のみで遊べる)
- **実装規模感**: 本格構成(TypeScriptモノレポ+テスト付き)。将来的なカード追加・ルール調整のしやすさを優先
- **観戦機能**: 最大2人まで観戦できるようにする。対戦者と同じルームURLで自動的に観戦者として割り当てる方式

正体隠匿ゲームのため、「相手の手札」「相手の未オープンアンドロイドカードの正体(ANDOROID/ERROROIDどちらか、さらにはどのキャラか)」を絶対にクライアントに漏らさない設計が最重要要件。観戦者にも同様に両者の秘密情報を一切見せない。

## ゲームルール(写真から書き起こし・要点)

### 勝利条件
相手の「エラーロイド」(裏面が犯人)カードを先にオープンさせたら勝ち。

### コンポーネント
- アンドロイドカード: 7キャラ(齋藤さんNo.1～No.7)。各キャラにつき「裏がANDOROID版」「裏がERROROID版」の2バリエーションが存在(全28枚、各キャラ4枚)
- 推理カード: 9種・山札40枚 + リヴィールカード「真相解明」(先攻用1枚コスト13・後攻用1枚コスト12、山札に含まれない別カード)
- 推理力トークン20個、証拠トークン14個

### 準備〜配置
1. 各プレイヤーに、7キャラ分の「裏ANDOROID版」7枚+「裏ERROROID版」7枚=14枚を配る
2. 先攻/後攻を決め、それぞれ対応する「真相解明」をリヴィールエリアに置く
3. 各プレイヤーは自分の14枚のうち、まず裏ANDOROID版7枚(7キャラ1枚ずつ)を並べる準備をし、そのうち**1キャラだけ**を裏ERROROID版に差し替える。つまり場に並ぶのは7キャラ各1枚(計7枚)、うち1枚だけが真犯人。残り7枚(未使用分)は除外。相手に正体が分からないよう伏せて左からカードナンバー順に並べる
4. 山札から4枚引いて手札にしてゲーム開始(その後ドローで5枚になる)

### ターンの流れ(交互)
相手のエラーロイドがオープンされるまで繰り返す:
1. **ドロー**: 手札が5枚になるまで山札から引く(山札切れ時は廃棄場をシャッフルして山札に)
2. **インストール**: インストールエリアから推理力を1個、自分のメモリーエリア(上限10個)へ
3. **シンキングタイム**: メモリーエリアの推理力を消費エリアへ移し消費して、以下を好きな順・何度でも実行:
   - 手札の推理カードを使用(コスト分の推理力を払い公開・効果発動、使用後は廃棄場へ)
   - 自分の場の**未オープン**アンドロイドカードを使用(コスト分の推理力を払いオープンして効果発動)
   - 手元のリヴィールカード「真相解明」を使用(コスト分払い使用宣言、使用後はカードを裏返して以降そのゲーム中は使用不可。廃棄場には送らず、「証拠回収」による再取得の対象にもならない)。**コストは自分の場でオープン済みのアンドロイド枚数だけ動的に減少**
4. **エンド**: 消費エリアの推理力を全てメモリーエリアへ戻し、ターン終了

### 推理力・証拠トークンの循環ルール(確定)
- **推理力**: インストールエリア(初期20個)→(インストールで)自分のメモリーエリア→(シンキングタイムの消費で)消費エリア→(エンドフェーズで)自分のメモリーエリアへ全戻し、のクローズドループ。推理力トークンは消滅しない。総量20個=プレイヤー2人×メモリーエリア上限10個、とちょうど一致するため、理論上インストールエリアが枯渇することはない(各プレイヤーが上限10個まで貯めた時点で合計が20個に達するため)。デジタル版では「インストールエリアの残数」を厳密管理せず、各プレイヤーのメモリーエリアに上限10個のカウンターを持たせるだけで等価に再現できる
- **証拠**: 物理コンポーネントは証拠エリアに14個だが、**デジタル版では総量制限を設けず、各カードの証拠値を無制限カウンターとして扱うことを確定**(ユーザー決定済み)。「証拠が足りず効果が発動できない」というルールは存在しない

### オープン条件(3パターン)
1. 付与された証拠がアリバイ(体力)以上になった時、即座に(効果発動中でも)強制オープン
2. 一部のカード効果により証拠に関係なく強制オープン
3. 自分のターン中、自分の未オープンカードを自らオープンして効果を使う場合

### 対象選択の主体と対象範囲(実装上の重要ルール・確定)
効果テキストは「選択の主体(誰が選ぶか)」と「対象の場(どちらの場のカードから選ぶか)」が独立した2軸になっている。
- 文中の**「自分」「相手」**(自分=青背景、相手=赤背景)は**選択する主体**を示す。「自分は」ならカード使用者本人が選ぶ、「相手は」なら対戦相手が(相手自身の意思で)選ぶ
- 文中の**「アンドロイド」アイコンの背景色**は**対象の場**を示す(カード使用者から見て): 赤背景=対戦相手の場、青背景=自分の場
- 上記2軸は独立しているため、「相手(主体)が、相手自身の場(赤)から選ぶ」(=相手に自分の手持ちカードの中から選ばせる)という組み合わせも存在する。書き起こしたカードの大半はこのパターン(相手主体+相手の場=結果的に相手が自分の場から選ぶ)か、その逆(自分主体+相手の場=通常の攻撃的選択)であり、**対象の場が「自分の場(青)」になるのは弁護士の①ステップと痕跡発見の②ステップの2箇所のみ**(いずれも自分自身の疑い・証拠を自分で調整するステップ)。アイビーは実物カードで確認した結果、通常パターン(自分主体+相手の場)であり例外ではない

### 強制オープンとカード効果発動の関係(確定)
- 強制オープン(証拠がアリバイ以上)時、通常のアンドロイドカードは**カード効果を発動しない**(単に正体が公開されるだけ)。カード効果はパターン3(自分の意志で未オープンカードをコストを払ってオープンする場合)のみ発動する。唯一の例外はオリバーで、相手ターン中に証拠が丁度4になった時だけ専用の常在効果が発動する
- 強制オープンされたカードがエラーロイドだった場合は、原因(証拠閾値到達か、カード効果による強制オープンか)を問わず即座にゲーム終了。効果は一切発動しない

### 重要なFAQルール
- 発動条件を満たさないカードも使用行動自体は可能(コストを払い、廃棄場へ送る/オープンする)が効果は発動しない。条件を満たす場合は効果発動を拒否できない
- カード効果解決中に他カード効果の割り込みは不可
- 相手に真偽を答えさせる効果(ベン、尋問)で嘘の申告は不可(デジタル版ではサーバーが自動判定すればよい)
- エラーロイドを効果発動のためオープンした場合、効果は発動せずその時点で即座にゲーム終了・敗北確定
- 証拠は0を下回らない

### カード効果テキスト(現物カードでユーザー確認済み。以下を実装時の正本とする)

**アンドロイドカード7種**(左上=コスト、左下=カードNo、右下=アリバイ。選択主体/対象の場は上記ルールに基づく分析):
| キャラ | コスト | カードNo | アリバイ | 選択主体→対象の場 | 効果 |
|---|---|---|---|---|---|
| 齋藤さん(探偵)_No.1 | 3 | 1 | 5 | 相手→相手自身の場 | 相手はアンドロイドを1体選択し、それをオープンする |
| 齋藤さん(弁護士)_No.2 | 2 | 2 | 5 | ①自分→自分の場 ②相手→相手自身の場 | 自分はアンドロイドを1体選択し、それにある証拠を0になるまでマイナスする。その後、相手はアンドロイドを1体選択し、それの証拠を先ほどマイナスした数だけプラスする |
| 齋藤さん(美大生)_No.3 | 3 | 3 | 5 | 自分→相手の場 | 自分はアンドロイドを1体選択し、それに証拠+1。これを3回繰り返す(同じアンドロイドを複数回選択しても良い) |
| 齋藤さん(バーテンダー)_No.4 | 2 | 4 | 5 | 相手→相手自身の場(全体から検索) | 相手は証拠が2以上あるアンドロイドの中に、エラーロイドが「いる」か「いない」か答える(証拠2以上のアンドロイドがなければ発動しない) |
| 齋藤さん(メイド)_No.5 | 2 | 5 | 5 | (対象選択なし、自分の推理力操作のみ) | 自分の消費エリアにある推理力を自分の手札と同じ数だけメモリーエリアに戻す(手札が5枚なら戻せる推理力も5) |
| 齋藤さん(マジシャン)_No.6 | 0 | 6 | 6 | (常在トリガー、対象選択なし) | 自分のターン中のオープンでは効果不発動。相手ターン中に証拠が変動し、このカードにある証拠が丁度4になった時オープンする。その後、相手の全アンドロイドは証拠+1 |
| 齋藤さん(猫)_No.7 | 4 | 7 | 5 | 相手→相手自身の場 | 相手はアンドロイドを1体選択し、それに証拠+4。これを2回繰り返す(同じアンドロイドを複数回選択しても良い) |

**推理カード9種・山札40枚**:
| カード名 | コスト | 枚数 | 選択主体→対象の場 | 効果 | 状態 |
|---|---|---|---|---|---|
| 追跡 | 3 | 4 | 自分→相手の場 | 自分はアンドロイドを1体選択し、それに証拠+2 | 確定 |
| 単独不審 | 2 | 5 | 相手→相手自身の場 | 相手はアンドロイドを1体選択する。それに1以上の証拠が既にあれば証拠+4、証拠がなかった場合は証拠+2 | 確定 |
| 集団ヒステリー | 3 | 5 | ①自分→相手の場(3体) ②相手→同じ3体の中から2体 | 自分はアンドロイドを3体選択。相手はその中から2体選択し証拠+2(3体選択できなければ不発動) | 確定 |
| 決定的証拠 | 4 | 2 | 相手→相手自身の場 | 相手はアンドロイドを1体選択し、証拠+5 | 確定 |
| 尋問 | 1 | 3 | 相手→相手自身の場 | 相手は証拠1以上のアンドロイドを1体選択する。それがエラーロイドで「ある/ない」を答える(証拠が1以上あるアンドロイドがいなければ不発動) | 確定 |
| 犯行動機の把握 | 2 | 6 | 相手→相手自身の場 | 相手はアンドロイドを1体選択し、証拠+3 | 確定 |
| 痕跡発見 | 1 | 10 | ①相手→相手自身の場(+1) ②自分→自分自身の場(-1) | 相手はアンドロイドを1体選択し証拠+1、その後自分はアンドロイドを1体選択し証拠-1 | 確定 |
| 証拠回収 | 2 | 3 | (対象はカードでなく廃棄場、自分が選び相手に見せる) | 廃棄場から好きなカードを1枚選び、相手に見せてから自分の手札に加える(廃棄場にカードがなければ不発動) | 確定 |
| データ整理 | 1 | 2 | (対象選択なし、自分の手札操作のみ) | 自分の手札を全て廃棄場に送り、その後手札が5枚になるよう山札から引く | 確定 |

**リヴィールカード「真相解明」**: 自分はアンドロイドを1体選択しオープンする(自分→相手の場。実装時にユーザー確認済み、当初案の「自分→自分の場」から修正)。コストは自分の場のオープン済みアンドロイド数だけマイナス(先攻13/後攻12が基準値)。

**確認状態は3段階で統一する**(以前の版で表記が食い違っていたため一本化):
- **確定**: ユーザーが実物カードで文言を照合済み。そのまま実装
- **高確度・未確認**(該当: 決定的証拠、データ整理): 読み取りの自信は高いが実物照合はまだ。`isStub`は付けず通常実装するが、初回プレイテスト時に実物カードで確認する
- **要確認**(該当: 痕跡発見のみ): 数値・選択主体とも自信が持てない。`isStub: true`を付けて仮テキストで実装し、後から差し替え可能にする。着手前後どこかのタイミングでユーザーに実物カードでの最終確認を依頼する

## 技術方針

### 構成・スタック
- **言語/構成**: TypeScript、npm workspacesによるモノレポ(`packages/shared` `packages/server` `packages/client`)
- **クライアント**: Vite + React + TypeScript、状態管理はzustand
- **テスト**: Vitest(sharedのカード効果ロジック、serverのGameEngineを中心にユニット/統合テスト)

### デプロイ・接続
- **サーバー**: Node.js + Express + Socket.io。ローカルPC起動ではなく**クラウドへ常時デプロイ**(Render想定。無料のWeb Service枠でNode.js+WebSocketが動く。Railway等でも可)し、固定URL(例: `https://erroroid.onrender.com`)を友人にそのまま伝えてブラウザで開けば参加できるようにする。GitHubリポジトリにpushすると自動デプロイされる構成にする。Render無料枠は一定時間アクセスがないとスリープする仕様のため、**遊ぶ前に一度URLを開いて起動を待つ**(コールドスタートで数十秒かかる場合がある)運用上の注意点をREADMEに明記する。**無料枠のスリープ挙動・提供条件はRender側の外部要因で時期により変わりうるため、ステップ5でデプロイ設定に着手する直前に公式ドキュメントの最新情報を確認する**(計画時点と仕様が変わっていた場合、ハートビート戦略を見直す)
- **ルーム保護**: インターネット上に公開されるため、`RoomManager`は**ルームURLに推測困難なランダムトークンを付与**(例: `/room/xxxxxxxx`)。トップページからは誰でも新規ルームを作れるが、友人にはそのルーム固有URLだけを共有する、という軽量な保護を入れる(本格的なログイン認証までは不要と判断)
- **観戦機能**: 同じルームURLに接続した3・4人目は自動的に**観戦者(最大2人)として割り当て**、5人目以降は拒否する。観戦者は操作不可の読み取り専用。座席はシンプルに接続順(先着順)で決める(意図した友人より先に別の人がプレイヤー枠に入る可能性は理論上あるが、身内限定プレイのため実害は小さいと判断し、明示的な招待/承認フローまでは設けない)。観戦者が切断した場合はその枠を即座に解放し、次の接続者に再利用可能にする
- **運用上の注意(README明記)**: GitHub pushで自動デプロイされる構成のため、**対戦中にpushするとサーバープロセスが再起動しGameStateが消える**。対戦中は開発・pushを行わない旨をREADMEに明記する

### 接続の安定性設計(クラウド常時稼働特有の課題)
「シンキングタイム」は数分〜十数分かかりうるフェーズであり、その間WebSocket接続はほぼ無通信になる。Render無料枠は一定時間HTTPアクセスがないとスリープする仕様だが、**アイドル中のWebSocket接続自体がスリープ判定上「アクティビティ」として扱われるかは未検証**(Renderの挙動として保証された仕様ではなく、確認が必要)。長考中にサーバーがスリープ/再起動し、インメモリのGameStateが失われるリスクに対して、以下を設計として組み込む:
- **ハートビート**: クライアントから数分おき(例: 4分間隔)にHTTPエンドポイント(WebSocketフレームだけでなく実際のHTTPリクエスト)へ軽量なpingを送り、サーバーを「アクティブ」扱いさせてスリープを予防する。これは通信・同期の実装(開発ステップ3)に含め、**最終テスト段階(ステップ5)まで確認を先送りしない**
- **再接続の設計**(単なる動作確認ではなく仕様として定義): 各プレイヤー/観戦者はルーム参加時に**セッショントークン**(ランダムなUUID)を発行され、クライアント側(localStorage等)に保持する。ソケット切断→再接続時はこのトークンを送り、`RoomManager`はトークンから座席(プレイヤー1/2、観戦者N)を引き当てて`socketId`を差し替え、最新のマスク済みビューを再送する。プレイヤーの座席は切断後も一定時間(初期値5分。自宅回線の不調・モデム再起動等も考慮し運用しながら調整してよい)予約保持し、他人に奪われないようにする
- **座席予約タイムアウトと本当のサーバー再起動を区別してエラー表示する**: 単に「トークンが見つからない」だけだと、5分の予約期限切れで戻ってきた本人にも「サーバーが再起動された」という不正確なエラーが出てしまう。これを避けるため、`RoomManager`は予約期限切れの座席を即座に完全削除せず、**しばらくの間(例: 1時間)「期限切れ」であることを示すtombstoneレコード**として残す。再接続時にtombstoneがヒットすれば「座席の予約期限が切れました。もう一度参加し直してください」、tombstoneも含めて一切情報が見つからなければ「サーバーが再起動されたためゲームが失われました。新しく部屋を作り直してください」と、原因ごとに異なるメッセージを出し分ける(後者は真にプロセス再起動でメモリが全損したケースに限定される)
- **状態消失時の扱いを明示的に切り分ける**: (a) ネットワーク瞬断やブラウザ再読み込みなど「サーバープロセスは生きている」場合は上記トークン方式で完全復帰できる。(b) Renderのスリープ/再起動やデプロイなど「サーバープロセス自体が再起動した」場合はインメモリのGameStateは失われる。無料枠のみで小規模友人プレイという前提を踏まえ、(b)のケースでは無理に永続化(DB導入等)せず、上記の通り明示的にエラー表示する割り切った設計にする(スリープが実運用で頻発するようであれば、将来的にRedis等での永続化を追加検討)

### 秘匿情報設計(最重要)
- `GameState`(手札内容、両者の全アンドロイドの`identity`、山札の並び)はサーバーメモリにのみ存在。クライアントには`ViewSerializer`でパースペクティブ別(プレイヤー1/プレイヤー2/観戦者)にマスクしたビューのみを個別emit(broadcastしない)
- プレイヤー視点: 自分の手札・自分の未オープンカードの正体は見える。相手の手札は枚数のみ、相手の未オープンカードは`character`すら伏せる
- 観戦者視点: **両プレイヤーの手札・未オープンカードの正体をどちらも伏せた「公開情報のみ」のビュー**(オープン済みカード、証拠値、廃棄場、行動ログ、手番など)を送る(観戦者が対戦者と会話できる前提のため、どちらか一方の情報が漏れると成立しなくなる)
- クライアント側はゲームロジックを一切計算・保持しない(操作の「意図」をサーバーに送り、結果はサーバーが計算して送り返す)。観戦者クライアントは対象選択モーダルやアクションボタンを非表示にした読み取り専用UIにする

### カード効果システム
汎用エフェクトDSL(`selectTarget`/`addEvidence`/`setEvidence`/`forceOpen`/`drawCard`/`discardCard`/`peekHand`/`recoverFromDiscard`/`askTruth`(サーバー自動判定)/`conditionalFlag`/`custom`逃げ道)で宣言的にカードを定義。**`selectTarget`ステップは`chooser`(誰が選ぶか: `'self' | 'opponent'`)と`board`(どちらの場から選ぶか: `'ownerSelf' | 'ownerOpponent'`、カード所有者基準)を独立したパラメータとして持つ**(例: charlotte(齋藤さん(探偵))は`chooser:'opponent', board:'ownerOpponent'`=相手が相手自身の場から選ぶ。追跡は`chooser:'self', board:'ownerOpponent'`=自分が相手の場から選ぶ。`board:'ownerSelf'`になるのはlucas(齋藤さん(弁護士))の1段目と痕跡発見の2段目のみ)。この分離により新カード追加時の実装ミスを防ぐ

各カード定義には`confidence: 'confirmed' | 'high' | 'stub'`という軽量なメタ情報を持たせる(`isStub`だけでなく確認状態を3段階すべて記録)。これにより実物カードでの確認状態を一覧・棚卸しできる。全カードの実物照合が完了し、現在は全カード`confirmed`(2026-07-25時点、詳細は`docs/cards.md`参照)

### ゲーム進行エンジン
`GameEngine.dispatch(action)`を唯一のエントリポイントとする自前ステートマシン(setup→draw→install→thinking→end→…→gameOver)。`ActionValidator`で手番・フェーズ・コスト・対象の正当性を検証(観戦者からの操作は拒否)。強制オープン判定は証拠変化のたびに`checkForceOpen`を通す。**このチェックはカードをオープン(公開)するだけで、通常のアンドロイドカードの`onOpenEffect`は発動させない**(効果はパターン3=自発的コスト支払いによるオープン時のみ発動、という原則をエンジンで固定する)。ERROROIDが強制オープンされた場合は原因を問わず即`gameOver`へ遷移し、以降の効果解決を打ち切る。オリバーの常在トリガーのみ`TriggeredAbility`として`checkForceOpen`とは別枠で「相手ターン中に証拠が丁度4になった瞬間」を監視する専用ロジックを持つ。効果解決中の割り込み不可はEffectResolverの単一キュー実行で担保。リヴィールコストは`computeRevealCost(state, player)`で都度動的計算

## ディレクトリ構成(初期)

```
エラーロイド/
  package.json                      (workspaces root)
  packages/
    shared/src/
      types/core.ts
      effects/script.ts / conditions.ts
      cards/android/{charlotte,lucas,ivy,ben,lillian,oliver,saint}.ts
      cards/deduction/index.ts      (9種40枚、各カードにconfidence: 'confirmed'|'high'|'stub'を付与)
      cards/reveal/index.ts
    server/src/
      index.ts                      (Express+Socket.io起動、PORT環境変数対応)
      game/{GameEngine,GameState,ActionValidator,EffectResolver,ForceOpenChecker,Deck,Board,Tokens}.ts
      cards/customHandlers/*.ts
      network/{SocketHandlers,ViewSerializer,RoomManager}.ts
    server/test/{effects,engine}/*.test.ts
    client/src/
      main.tsx / socket.ts
      store/useGameStore.ts
      components/{Board,Hand,AndroidRow,TokenTray,TargetSelectModal,ActionLog}.tsx
      assets/cards/                 (現物カード写真から切り出した画像。.gitignore対象)
  docs/{rules.md, cards.md}         (書き起こし正本。要確認箇所を明記)
  render.yaml                       (Renderへのデプロイ設定。ビルド/起動コマンドを定義)
```

## 開発ステップ

優先順位は「UIが無くても対戦の正しさをコードで担保できる状態」を先に作ることを重視する(1〜3は見た目ゼロでもVitest/モックソケットで進行・検証可能。UIは4番目で、それまでの正しさの上に乗せるだけにする)。目安規模はS(数時間〜半日)/M(1〜2日)/L(2〜4日)程度の相対感。

1. **盤面・トークン基盤**(規模目安: S〜M、UIなし・Vitestで検証): `GameState`型、Deck/Board/Tokensの素の操作関数(証拠付与、推理力インストール/消費、オープン判定)。「証拠がアリバイ以上で強制オープン」「証拠は0未満不可」「推理力20個⇔メモリー上限10個のクローズドループ」をユニットテストで固める
2. **カード効果システム**(規模目安: L、最も工数がかかる山): EffectScript DSL(chooser/board分離)とEffectResolver実装。書き起こし済みのアンドロイド7種・推理カード9種をshared配下にデータ定義。要確認1枚(痕跡発見)はスタブ実装。カードごとに入力状態→効果適用→期待出力のユニットテスト。ここが正しく動けば「コンソール上で1ゲーム完走できる」状態になるので、実質的な最初のマイルストーン
3. **通信・同期**(規模目安: M〜L): Socket.io導入、RoomManagerでルームトークン発行・2人合流→開始・3〜4人目は観戦者として自動割り当て・5人目以降拒否。ViewSerializerで秘匿マスク(プレイヤー視点2種+観戦者視点)を実装し個別emit。ActionValidator経由でクライアント操作をGameEngineへ橋渡し(観戦者からの操作は拒否)。**このステップで「セッショントークンによる再接続」「クライアントからのハートビートping」を仕様として実装する**(動作確認だけをステップ5に先送りしない)。モックソケットでの自動プレイ統合テスト(全カード効果を1回は通す。観戦者視点に手札・正体情報が含まれないことも確認。ソケット切断→再接続で座席とGameStateが正しく復元されることも確認)
4. **UI/UX**(規模目安: L): React+Viteで盤面・手札・7枚のアンドロイド行・トークントレイ・対象選択モーダル・行動ログ。観戦者モードでは操作系UIを非表示にした読み取り専用表示に切り替え。再接続失敗時の「ゲームが失われました」エラー表示も実装。デザインより情報の正確さ・見やすさを優先
5. **デプロイ・統合テスト・調整**(規模目安: S〜M): Renderへのデプロイ設定(render.yaml、PORT環境変数対応、ビルドコマンド)、GitHub連携での自動デプロイ確認。実際に2台の別々のPC(別回線)からルームURLで接続して対戦確認。**特に「シンキングタイムを模して10分以上操作せず放置してもスリープ/切断しないか」をハートビート込みで明示的に検証する**(コールドスタート挙動、通常の再接続挙動も含む)。FAQルール(発動条件不足でも使用行動は可/発動拒否不可/割り込み不可/自分のエラーロイドを効果でオープンすると即敗北)を個別テストケース化。要確認カードのテキスト確定後にスタブを差し替え

## 検証方法

- **単体テスト(Vitest)**: sharedのカード効果ロジック、serverのGameEngine/ForceOpenCheckerをUIなしで検証
- **統合テスト**: モックソケット2クライアント(+観戦者クライアント)で1ゲームを最初から最後まで進行させ、勝敗判定・秘匿情報の非漏洩(相手クライアント、および観戦者クライアントへのJSONに`identity`や未オープン時の`character`・両者の手札内容が一切含まれないことをアサート)を確認
- **手動デプロイ確認**: Renderへデプロイ後、別々のネットワークにある実機2台からルームURLでアクセスし対戦。コールドスタート挙動、通常の切断/再接続に加え、**長時間(10分以上)操作せず放置した場合にスリープ・GameState消失が起きないか**をハートビート込みで確認
- **ルール適合チェックリスト**: `docs/rules.md`の書き起こしと実装を突き合わせ、特にFAQ項目にチェックを入れる
